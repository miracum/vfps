@page "/"
@using Microsoft.Extensions.Options
@using Vfps.Config
@inject IOptions<S3Config> S3ConfigOptions
@inject IStringLocalizer<SharedResources> Localizer

<PageTitle>Vfps</PageTitle>

<div class="bb:mx-auto bb:space-y-6">
    <div>
        <h1 class="bb:text-2xl bb:font-semibold bb:tracking-tight">Vfps</h1>
        <p class="bb:text-muted-foreground">
            @Localizer["App.Home.Subtitle"]
        </p>
    </div>

    <div class="bb:grid bb:gap-4 bb:sm:grid-cols-2">
        <BbCard>
            <BbCardHeader>
                <div class="bb:flex bb:items-center bb:gap-2">
                    <LucideIcon Name="database" Size="18" Class="bb:text-primary" />
                    <BbCardTitle>@Localizer["App.Nav.Namespaces"]</BbCardTitle>
                </div>
                <BbCardDescription>
                    @Localizer["App.Home.NamespacesDescription"]
                </BbCardDescription>
            </BbCardHeader>
            <BbCardFooter>
                <BbButton Href="/ui/namespaces" Variant="ButtonVariant.Outline">
                    @Localizer["App.Common.Open"]
                </BbButton>
            </BbCardFooter>
        </BbCard>

        @if (S3ConfigOptions.Value.IsEnabled)
        {
            <BbCard>
                <BbCardHeader>
                    <div class="bb:flex bb:items-center bb:gap-2">
                        <LucideIcon Name="file-spreadsheet" Size="18" Class="bb:text-primary" />
                        <BbCardTitle>@Localizer["App.Nav.CsvProcessing"]</BbCardTitle>
                    </div>
                    <BbCardDescription>
                        @Localizer["App.Home.CsvProcessingDescription"]
                    </BbCardDescription>
                </BbCardHeader>
                <BbCardFooter>
                    <BbButton Href="/ui/csv-jobs" Variant="ButtonVariant.Outline">
                        @Localizer["App.Common.Open"]
                    </BbButton>
                </BbCardFooter>
            </BbCard>
        }
    </div>
</div>
