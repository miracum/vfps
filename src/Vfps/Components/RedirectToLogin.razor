@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo(NavigationManager.BuildLoginUrl(), forceLoad: true);
    }
}
